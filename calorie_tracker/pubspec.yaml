name: calorie_tracker
description: "A new Flutter project."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  firebase_core: ^3.12.1
  firebase_auth: ^5.5.1
  cloud_firestore: ^5.6.5
  fl_chart: ^0.71.0
  intl: ^0.20.2
  http: ^1.3.0
  flutter_dotenv: ^5.0.2
  pie_chart: ^5.4.0
  image_picker: ^1.0.7
  path_provider: ^2.0.11
  image: ^4.0.15
  shared_preferences: ^2.0.15
  provider: ^6.0.5
  flutter_cache_manager: ^3.3.0
  flutter_image_compress: ^2.4.0
  dio: ^5.4.1
  mobile_scanner: ^4.0.0
  # Add any additional dependencies you might need here

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/food_and_calories.json
    - .env
